### 🔰 Task 1. Dashboard (Главный экран)

**Назначение:**  
Центральная точка приложения. Пользователь включает защиту, наблюдает статус и получает доступ к модулям.

---

#### UI элементы:
- Большая центральная кнопка-щит (`UIButton` / `ClayButton`).
- Текст статуса: “Protection is OFF / ON”.
- 4 плитки-модуля: SmartGuard, Ad Cleaner, Privacy Shield, Safe Browsing.
- Нижняя навигация (TabBar).

---

#### Поведение и логика:

1. **Первый запуск:**
   - Проверяется `isProUser` (из UserDefaults или Apphud).
   - Если `false`:
     - статус = “Protection is OFF”;
     - все плитки заблокированы (opacity 0.6, без взаимодействия).
   - Если `true`:
     - статус = “Protection is ON”;
     - плитки активны (tap-enabled).

2. **Нажатие на большую кнопку-щит:**
   - Если `isProUser == false` → открыть `PaywallView`.
   - Если `isProUser == true`:
     - включается `protectionEnabled = true`;
     - UI меняет цвет щита на зелёный (`#00C27A`);
     - запускается лёгкая пульсация (анимация защиты активна);
     - выполняются действия модулей:
       - активируется AdBlock (см. Task 2),
       - запускается авто-скан Wi-Fi (см. Task 3),
       - SmartGuard становится активен.

3. **Визуальные эффекты при активации:**
   - Волновая анимация по фону (радиальная от центра);
   - Лёгкий haptic feedback;
   - Звук мягкого “попа” (опционально).

4. **Нажатие на любую плитку модуля:**
   - Если `!isProUser` → Paywall;
   - Если Pro:
     - SmartGuard → открыть экран анализа;
     - Ad Cleaner → отобразить ON/OFF switch (toggle);
     - Privacy Shield → экран с cookie/trackers блокировкой;
     - Safe Browsing → экран parental control (в будущем).

5. **Состояния:**
   - OFF: серый фон, неактивные плитки.
   - ON: зелёный фон, активная защита.
   - Transition: анимация 0.5s.

---

#### Acceptance Criteria:
✅ Все UI-элементы реагируют на нажатия.  
✅ Paywall открывается при попытке включить защиту без подписки.  
✅ При включении (Pro) — активируются подмодули, появляется анимация.  
✅ Статус “Protection ON” отображается корректно.  
✅ Состояние сохраняется между сессиями.