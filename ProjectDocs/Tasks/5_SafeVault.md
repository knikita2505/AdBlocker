### 🔒 Task 5. Safe Vault

**Назначение:**  
Приватное хранилище с Face ID.

---

#### Поведение и логика:

1. **Первый вход:**
   - Проверка `isProUser`.
   - Если `false` → Paywall.
   - Если `true` → Face ID через `LAContext`.

2. **Face ID сценарий:**
   - Если успешно → отображается список “Secret Album” и “Secret Contacts”.
   - Если ошибка → “Authentication failed” и возможность повторить.

3. **Fake Vault:**
   - Если пользователь Pro:
     - доступна кнопка “Fake Vault”;
     - показывает пустой экран с “No items found”.

4. **Безопасность:**
   - При выходе из приложения → Face ID сбрасывается.
   - При возвращении → запрос повторно.

---

#### Acceptance Criteria:
✅ Face ID авторизация работает.  
✅ Ошибки обрабатываются корректно.  
✅ Fake Vault доступен после подписки.  
✅ Сессия сбрасывается после сворачивания.