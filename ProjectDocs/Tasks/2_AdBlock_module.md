### 🧹 Task 2. AdBlock Module

**Назначение:**  
Фильтрация рекламы и трекеров через Safari Content Blocker.

---

#### Поведение и логика:

1. **Активация:**
   - При включении защиты (см. Dashboard) → вызывает метод `activateAdBlocker()`.
   - Метод активирует встроенный Safari Content Blocker Extension.
   - В БД (`UserDefaults`) сохраняется `adblockEnabled=true`.

2. **Деактивация:**
   - При выключении защиты или вручную → `adblockEnabled=false`;
   - Safari Extension выгружается.

3. **Проверка состояния:**
   - При каждом запуске Dashboard проверяется значение `adblockEnabled`.

4. **Возможные статусы:**
   - “Active” — зелёная галочка.
   - “Disabled” — серый индикатор.
   - “Updating” — крутящийся loader (при обновлении фильтров, mock).

---

#### UI логика:
- Пользователь нажимает на “Ad Cleaner” плитку:
  - если не Pro → Paywall.
  - если Pro:
    - появляется toggle (ON/OFF);
    - при ON: показывает “Ads are being blocked”.

---

#### Acceptance Criteria:
✅ Переключатель работает, статус сохраняется.  
✅ При первом включении → активируется Safari Extension.  
✅ При выключении защиты → выключается.  
✅ Без подписки — Paywall.