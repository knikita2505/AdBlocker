### 🌐 Task 3. Wi-Fi Security

**Назначение:**  
Проверяет текущую сеть и выдаёт уровень безопасности.

---

#### Поведение и логика:

1. **На экране Wi-Fi Security:**
   - Пользователь нажимает “Scan Network”.
   - Метод `WiFiScanManager.scan()`:
     - Запускает `NWPathMonitor`.
     - Проверяет параметры соединения (mock).
     - Через 2 сек возвращает результат:
       - 50% — “Wi-Fi Secure” (зелёный);
       - 50% — “Unsecure” (красный).

2. **Результат:**
   - UI обновляется:
     - Цвет иконки Wi-Fi.
     - Текстовое сообщение (“Your network is safe” / “Unsecure connection detected”).
   - При “Unsecure” → появляется кнопка “Enable Auto Protection” (Paywall при `!isProUser`).

3. **Поведение при активации защиты с Dashboard:**
   - Wi-Fi проверяется автоматически раз в 30 секунд (mock timer).
   - Результат отображается в Dashboard (иконка Wi-Fi).

---

#### Acceptance Criteria:
✅ Кнопка “Scan Network” работает.  
✅ Через 2 сек появляется результат.  
✅ Цвет и статус корректно отображаются.  
✅ “Auto Protection” вызывает Paywall при отсутствии подписки.